先验敏感性声明：
  结构学习阶段仅将领域先验作为起始边（start_dag），而非硬性白名单；因此结果主要由数据驱动，避免先验对结构的过度支配。

稳定边阈值声明：
  使用自举法（n=200）评估结构稳健性，本研究以出现频率≥0.70 判定为稳定边，论文只对稳定骨架进行解释。

外生性约束：
  YearBin 与 topic 被视为外生变量，模型禁止任何指向它们的入边（*→YearBin / *→topic）。

